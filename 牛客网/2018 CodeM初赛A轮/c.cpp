#include <iostream>
#include <stdio.h>
#include <string.h>
#include <vector>
#include <set>
#include <map>
#define INF 0x3f3f3f3f
#define ll long long
using namespace std;
const int N = 1e5+10;
const int mod = 1e9+7;
struct Nod{
    int u, v, w, l;
}nod[N];
int main() {
    int n, w, l;
    cin >> n;
    for(int i = 0; i < n-1; i ++) {
        scanf("%d%d%d%d", &u, &v, &w, &l);
        nod[]
    }

    return 0;
}
