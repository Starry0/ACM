#include <bits/stdc++.h>
#define ll long long
using namespace std;
const int N = 3e5+10;
int n, m;
int a[3][N];
bool vis[3][N];
int main() {
	cin >> n;
	ll sum = 0;
	for(int i = 1; i <= 2; i ++) {
		for(int j = 1; j <= n; j ++) {
			scanf("%d", &a[i][j]);
			sum += 1LL*a[i][j]
		}
	}
	m = 2*n;
	ll ans = a[1][1];
	int x = 1, y = 1, cnt = 2;
	sun -= a[1][1];
	while(cnt <= 2*n) {
		if(x == 1) {
			if(a[])
		} else {

		}
	}
	return 0;
}