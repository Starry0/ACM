#include <iostream>
const int maxn = 1e6+10;
using namespace std;
struct DATE{
    int num,t,left,right;
}da[maxn];
int main()
{
    int n,sum=0;
    for(int i=1;i<=n;i++){
        scanf("%d%d",&da[i].num,&da[i].t);
        sum+=t;
    }
    if(t%2!=0)
        cout<<-1<<endl;
    else{

    }
    return 0;
}
