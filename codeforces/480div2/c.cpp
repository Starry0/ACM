#include <bits/stdc++.h>
#define ll long long
using namespace std;
int a[260];
int main() {
    int n, k, p, j;
    cin >> n >> k;
    memset(a, -1, sizeof(a));
    for(int i = 1; i <= n; i ++) {
        cin >> p;
        if(a[p] != -1) printf("%d ",a[p]);
        else{
            for(j = )
        }
    }
    return 0;
}
