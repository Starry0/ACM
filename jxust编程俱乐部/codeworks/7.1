#include<iostream>
#include<cmath>
using namespace std;
int main()
{
	int N;
	cin>>N;
	for (int i=2;i<=N;i++)
	{
		int k=1;
		for (int j=2;j<=sqrt(i);j++)
		if (i%j==0)
		k=0;
		if (k==1)
		cout<<i<<endl;
	}
}