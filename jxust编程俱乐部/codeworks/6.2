#include <iostream>
using namespace std;
int main()
{
	char a;
	int b=0,c=0,d=0,e=0;
	while ((a=getchar())!='\n')
	{ if (a>='A'&a<='Z'||a>='a'&a<='z')
	 b++;
	 else if (a>='0'&&a<='9')
	 c++;
	 else if (a==' ')
	 
	 d++;
	 else e++;
	}
	printf ("%d %d %d %d",b,c,d,e);
	return 0;
}