#include<stdio.h>
#include<math.h>
int f(int x)
{
	int a,z=1;
	for (a=2;a<=sqrt(x);a++)
	{
		if (x%a==0)
		z=0;
	}
	return z;
}
int main()
{
	int a,n;
	scanf("%d",&n);
	a=f(n);
	if (a)
	printf("prime");
	else printf("not prime");
	return 0;
}