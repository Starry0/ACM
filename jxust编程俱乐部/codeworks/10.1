#include<iostream>
using namespace std;
int main()
{
	int a[3],*p,i;
	p=a;
	for(i=0;i<3;i++)
	cin>>*(p+i);
	if(*p<*(p+1))
	{
		if(*(p+1)<*(p+2))
		cout<<*p<<' '<<*(p+1)<<' '<<*(p+2)<<' ';
		else if(*p<*(p+2))
		cout<<*p<<' '<<*(p+2)<<' '<<*(p+1)<<' ';
		else cout<<*(p+2)<<' '<<*p<<' '<<*(p+1)<<' ';
	}
	else {
		if(*p<*(p+2))
		cout<<*(p+1)<<' '<<*p<<' '<<*(p+2)<<' ';
		else if(*(p+1)<*(p+2))
		cout<<*(p+1)<<' '<<*(p+2)<<' '<<*p<<' ';
		else cout<<*(p+2)<<' '<<*(p+1)<<' '<<*p<<' ';
	}
	return 0;
}