#include <stdio.h>
#include <math.h>
int main()
{
	float x ;
	scanf ("%f",&x);
	if (x<-1)
	printf ("%.6f",x*x*x-1);
	else if (x<=1)
	printf ("%.6f",-3*x+1);
	else if (x<=10)
	printf ("%.6f",3*exp(2*x-1)+5);
	else printf ("%.6f",5*x+3*log10(2*x*x-1)-13);
	return 0;
}