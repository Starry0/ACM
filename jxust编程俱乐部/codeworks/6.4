#include <stdio.h>
int f(int x)
{
	
	if (x==1) return 1;
	else{ x=x*f(x-1);return x;}
}
int main()
{
	int a,n;
	long long int sn=0;
	scanf("%d",&n);
	for (a=1;a<n+1;a++)
	{
		sn=sn+f(a);
	}
	printf ("%lld",sn);
	return 0;
}