#include <iostream>
#include <queue>
#include <stdio.h>
using namespace std;
string str;
int main() {
	while(true) {
		getline(cin, str);
		if(str[0] == '0') break;
		int ans = 0, tmp = 0;
		char chl
		for(int i = 0; i < str.length(); i ++) {
			if(isdigit(str[i])) {
				tmp = tmp*10+str[i]-'0';
			}
		}
	}
	return 0;
}