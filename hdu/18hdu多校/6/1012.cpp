#include <bits/stdc++.h>
#define ll long long
using namespace std;
const int N = 110;

int main() {
	int t;
	cin >> t;
	while(t--) {
		double a, b, x, y;
		cin >> a >> b >> x >> y;
		double d = atan(b/a);
		for(int k = 0; : k ++) {
			double ans = (1.0/(1.0*k*sin(2*d))) - tan(d*x)-1.0/(4.0*k*k*h*(sin()))
		}
	}
	return 0;
}