#include <bits/stdc++.h>
#define ll long long
using namespace std;
const int N = 1e5+10;
int t, n, m, l, r;
char s[N];
int main() {
	cin >> t;
	while(t--) {
		scanf("%d%d", &n, &m);
		scanf("%s", s);
		while(m--) {
			scanf("%d%d", &l, &r);
			
		}
	}
	return 0;
}