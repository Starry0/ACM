#include <iostream>
#include <stdio.h>
#include <string.h>
#define ll long long
using namespace std;
const int N = 1100;
char str[N];
ll x;
int main() {
	while(scanf("%lld %s", &x, str)!=EOF) {
        ll ans = 0;
        for(int i = 0; str[i]; i ++) {
            if(str[i] == '-') {

            }
        }
    }
	return 0;
}
