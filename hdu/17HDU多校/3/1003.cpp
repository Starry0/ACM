#include <iostream>
#include <stdio.h>
#define ll long long
using namespace std;
const int mod = 1e9+7;
const int N = 5e5+10;
int a[N];
int main() {
    int t;
    scanf("%d",&t);
    while(t--) {
        int n, k;
        scanf("%d%d",&n, &k);
        for(int i = 1; i <= n; i ++) scanf("%d", &a[i]);
        
    }
    return 0;
}
