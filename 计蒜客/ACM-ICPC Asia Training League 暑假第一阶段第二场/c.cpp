#include <bits/stdc++.h>
using namespace std;
const int N = 1010;
int x[N], y[N];
int main() {
	int t, n;
	scanf("%d", &t);
	while(t--) {
		scanf("%d", &n);
		for(int i = 0; i < n; i ++) scanf("%d%d", &x[i], &y[i]);
		
	}
	return 0;
}