#include <bits/stdc++.h>
using namespace std;
const int N = 2e4+10;
std::vector<int> vs[N];
std::vector<int> vs1[N];
int main() {
	int t, n, m, u, v;
	cin >> t;
	while(t--) {
		cin >> n >> m;
		for(int i = 1; i < n; i ++) {
			scanf("%d%d", &u, &v);
		}
		
	}
	return 0;
}